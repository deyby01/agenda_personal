# =============================================================================
# DJANGO CONFIGURATION
# =============================================================================

# Secret Key - IMPORTANT: Generate a new unique key for production!
# You can generate one at: https://djecrety.ir/ or using:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY='django-insecure-key-change-this-key-in-production'

# Debug Mode - Set to False in production for security
# When True: detailed error pages, no caching, verbose logging
# When False: generic error pages, optimized for performance
DEBUG=True

# =============================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# =============================================================================
# To send emails via Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password at: https://myaccount.google.com/apppasswords
# 3. Use that 16-character App Password below (not your regular Gmail password)
#
# Note: The system auto-detects SMTP when EMAIL_HOST is configured.
# If EMAIL_HOST is not set, emails will be printed to console (development mode).

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# GOOGLE OAUTH CONFIGURATION (Social Login)
# =============================================================================
# To enable "Sign in with Google":
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project (or select existing one)
# 3. Navigate to: APIs & Services > Credentials
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Select "Web application" as the application type
# 6. Add authorized redirect URIs:
#    - http://localhost:8000/accounts/google/login/callback/ (development)
#    - https://yourdomain.com/accounts/google/login/callback/ (production)
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
